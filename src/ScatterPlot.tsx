import React from 'react';

function ScatterPlot({
    xColumn,
    yColumn,
    xData,
    yData,
    labels,
    colors
}: {
    xColumn: string,
    yColumn: string,
    xData: number[],
    yData: number[],
    labels: string[],
    colors: string[]
}) {
    
    return (
        <>
        </>
    );
}

export default ScatterPlot;